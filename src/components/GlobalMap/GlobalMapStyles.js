import { colors } from '../../Colors'

export const styles = {
    footer: {
        height: '10%',
        flexDirection: 'row',
        justifyContent: 'space-around',
        backgroundColor: colors.primaryColor2
    }
};